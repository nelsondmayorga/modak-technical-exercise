import { HoverScaleDirective } from './hover-scale.directive';

describe('HoverScaleDirective', () => {
  it('should create an instance', () => {
    const directive = new HoverScaleDirective();
    expect(directive).toBeTruthy();
  });
});
